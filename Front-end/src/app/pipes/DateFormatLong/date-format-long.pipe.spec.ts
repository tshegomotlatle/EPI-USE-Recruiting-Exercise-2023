import { DateFormatLongPipe } from './date-format-long.pipe';

describe('DateFormatLongPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatLongPipe();
    expect(pipe).toBeTruthy();
  });
});
